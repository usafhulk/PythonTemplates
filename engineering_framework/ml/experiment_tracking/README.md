# Experiment Tracking Framework

Track ML experiments with params, metrics, and run lifecycle management.

## Features
- Run lifecycle (start/end with context manager)
- Parameter and metric logging
- Run listing by experiment
- MLflow-compatible interface
