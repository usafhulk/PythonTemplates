# Model Registry

Version and stage management for ML models across dev/staging/production.

## Features
- Register model versions with metrics
- Stage transitions (dev -> staging -> production)
- Latest version lookup by stage
- Extensible backend (swap for MLflow)
