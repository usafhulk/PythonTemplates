# Batch Processing Framework

Large dataset batch processing with configurable batch sizes and error tracking.

## Features
- Chunked iteration
- Per-batch error isolation
- Batch statistics
- Extensible BatchProcessor interface
