# Middleware Framework

Composable middleware pipeline for request/response processing.

## Features
- Chained middleware pipeline
- Built-in: logging, request ID, CORS
- Easy to extend with custom middleware
