# Request Validation Framework

Schema-based request validation with type checking, length constraints, and patterns.

## Features
- Field-level validation rules
- Type checking
- Length constraints
- Regex pattern matching
- Custom validators
