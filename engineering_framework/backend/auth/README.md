# Authentication System

JWT-based authentication with HMAC-SHA256 signing (no external dependencies).

## Features
- HMAC-SHA256 JWT creation and verification
- Token expiry validation
- User credential store
- Pluggable Authenticator interface
