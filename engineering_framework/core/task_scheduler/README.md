# Task Scheduler

In-process interval-based task scheduler with daemon thread execution.

## Features
- Add/remove jobs dynamically
- Interval-based triggers
- Error isolation per job
- Daemon thread (non-blocking)
