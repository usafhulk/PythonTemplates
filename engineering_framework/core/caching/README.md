# Caching Framework

Multi-level caching with TTL, decorator support, and pluggable backend.

## Features
- In-memory cache with TTL expiry
- `@cached` decorator
- Pluggable backends (swap for Redis)
- Key-based invalidation
