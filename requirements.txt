# Core (zero external dependencies — standard library only)
# Uncomment and install the extras you need.

# YAML config support
# pyyaml>=6.0

# Excel read/write support
# openpyxl>=3.1

# Development / testing
pytest>=7.0
pytest-cov>=4.0
